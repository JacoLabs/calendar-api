<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Calendar permissions -->
    <!-- READ_CALENDAR: For potential future conflict detection -->
    <!-- Note: WRITE_CALENDAR not needed as we use Intent.ACTION_INSERT which delegates to calendar app -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    
    <!-- Clipboard monitoring permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Package visibility queries for Android 11+ (API 30+) -->
    <queries>
        <!-- Query for calendar apps using INSERT action -->
        <intent>
            <action android:name="android.intent.action.INSERT" />
            <data android:mimeType="vnd.android.cursor.dir/event" />
        </intent>
        <!-- Query for calendar apps using EDIT action -->
        <intent>
            <action android:name="android.intent.action.EDIT" />
            <data android:mimeType="vnd.android.cursor.item/event" />
        </intent>
        <!-- Query for calendar apps using VIEW action -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="content" />
        </intent>
        <!-- Query for web browsers (fallback) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Query for common calendar apps by package name -->
        <package android:name="com.google.android.calendar" />
        <package android:name="com.samsung.android.calendar" />
        <package android:name="com.microsoft.office.outlook" />
        <package android:name="com.android.calendar" />
        <package android:name="com.htc.calendar" />
        <package android:name="com.lge.calendar" />
        <package android:name="com.sonyericsson.organizer" />
        <package android:name="com.motorola.blur.calendar" />
        <package android:name="com.oneplus.calendar" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CalendarEvent"
        tools:targetApi="31">

        <!-- Main Activity (minimal UI, mostly for settings) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CalendarEvent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Text Processor Activity for ACTION_PROCESS_TEXT -->
        <activity
            android:name=".TextProcessorActivity"
            android:exported="true"
            android:label="Create calendar event"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:process=":textprocessor">
            
            <!-- Primary intent filter for text processing -->
            <intent-filter android:priority="100">
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            
            <!-- Additional intent filter for broader compatibility -->
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.ALTERNATIVE" />
                <data android:mimeType="text/*" />
            </intent-filter>
            
            <!-- Metadata for better app recognition -->
            <meta-data
                android:name="android.support.text.processing"
                android:value="true" />
            <meta-data
                android:name="android.intent.action.PROCESS_TEXT.description"
                android:value="Extract calendar events from selected text" />
        </activity>

        <!-- Share Handler Activity for ACTION_SEND -->
        <activity
            android:name=".ShareHandlerActivity"
            android:exported="true"
            android:label="Create calendar event"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            
            <!-- Handle shared text -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Clipboard Monitor Service (optional background service) -->
        <service
            android:name=".ClipboardMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Notification Dismiss Receiver -->
        <receiver
            android:name=".NotificationDismissReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Quick Settings Tile (Android 7.0+) -->
        <service
            android:name=".CalendarEventTileService"
            android:exported="true"
            android:icon="@drawable/ic_launcher"
            android:label="Calendar Event"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

    </application>

</manifest>